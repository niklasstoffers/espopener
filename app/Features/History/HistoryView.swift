import SwiftUI

struct HistoryView: View {
    var body: some View {
        VStack {
            
        }
        .navigationTitle("History")
        .navigationBarTitleDisplayMode(.inline)
    }
}

#Preview {
    HistoryView()
}
